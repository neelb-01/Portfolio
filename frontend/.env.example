# Frontend env vars (none required for local dev)
# API URL is proxied through Vite — no VITE_API_URL needed locally

# If deploying to production, set this:
# VITE_API_URL=https://your-api-domain.com
